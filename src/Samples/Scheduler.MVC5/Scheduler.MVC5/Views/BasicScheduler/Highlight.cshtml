@{
    ViewBag.Title = "Scheduler | Basic initialization";
    ViewBag.SampleTitle = "Basic initialization";
    ViewBag.ShortDescription = "See how scheduler can be initialized";
    ViewBag.LongDescription = "Drag or double-click to create new event<br />Double-click on existing events to edit them, or drag them to change their time. ";
}

@Html.Raw(Model.GenerateJS())
@Html.Raw(Model.GenerateCSS())
<style>
        .highlight_section {
            opacity: 0.25;
            z-index:0;
            filter:alpha(opacity=25);
        } 
        .highlight_section:hover {
		    background-color: #90ee90;
	    }
    </style>

@Html.Raw(Model.GenerateHTML())